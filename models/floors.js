const mongoose = require('mongoose');
const Schema = mongoose.Schema;

// schema
const MySchema = new Schema({
    
    });

module.exports =  mongoose.model('rmWhere', MySchema, 'RevenuemanagerLocation')

